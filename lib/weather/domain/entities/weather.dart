class Weather{
  final int id;
  final int pressure;
  final String name;
  final String main;
  final String description;

  Weather(this.id, this.pressure, this.name, this.main, this.description);
}